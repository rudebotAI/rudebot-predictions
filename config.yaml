# Live config overrides applied on top of code defaults (env_config.py).
# Loosened 2026-06-24: min_ev_threshold 0.03->0.005, min_edge_bps 200->25 to surface candidates.
# Trades remain Telegram-gated; $2/trade and $10/day caps unchanged.
risk:
  min_ev_threshold: 0.005
  min_edge_bps: 25
